CREATE TABLE animals (id INTEGER PRIMARY KEY, name VARCHAR(64), type VARCHAR(64));

CREATE TABLE diets (id INTEGER PRIMARY KEY, animal VARCHAR(64), food VARCHAR(64));


-- insert some stuff into the tables
